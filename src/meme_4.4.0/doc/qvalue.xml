<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
                      "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd"
>
<html>
  <head>
    <title>qvalue</title>
     <link href="./meme-suite.css" rel="styleSheet" type="text/css" />
  </head>
  <body>
    <div id="main">
<script src="./meme-suite-logo.js" type="text/javascript">></script>
<h1>qvalue</h1>

<p><b>Usage:</b> <code>qvalue [options] &lt;pvalues&gt;</code></p>

<p><b>Description:</b></p>

<p>
Takes as input a list of p-values and prints a corresponding list of
q-values, computed using the method of Benjamini and Hochberg
(<i>Journal of the Royal Statistical Society B</i>, 57:289-300, 1995).</p>

<p><b>Input:</b></p> 

<p>
The input file contains one p-value per line.  The p-values do not
have to be in sorted order.
</p>

<p><b>Output:</b></p>

<p>
The output is two tab-delimited columns, p-value and q-value, sorted
by p-value.
</p>

<p><b>Options:</b></p>

<ul>

<li> <code>--null &lt;file&gt;</code>  Treat the primary input file
as raw scores, and begin by estimating p-values from the empirical
null scores given in this file.  The <code>--header</code> and
<code>--column</code> arguments apply to this file as well as the
primary input file.</li>

<li> <code>--pi-zero</code> Estimate the percentage of the
observed p-values that are drawn according to the null
hypothesis (known as pi<sub>0</sub> in the statistics
literature), and include this percentage in the FDR calculation.
By default, a fixed pi-zero of 1 is used.</li>

<li> <code>--pi-zero-file &lt;file&gt;</code> Print the estimated
value of pi-zero in the given file.  This option implies
the <code>--pi-zero</code> option as well.</li>

<li> <code>--fdr</code> Only compute FDR estimates.</li>

<li> <code>--bootstraps &lt;int&gt;</code> Number of bootstraps to
perform when estimating pi-zero.  Default=1000.</li>

<li> <code>--header &lt;int&gt;</code> Skip the specified number
of lines at the beginning of the given file.  Default=0.</li>

<li> <code>--column &lt;int&gt;</code> Read the p-value from the
specified tab-delimited column number.  Columns are
indexed starting from 1.  Default=1.</li>

<li> <code>--append</code> Print the input file to stdout, with an
additional, tab-delimited column containing the q-value.</li>

<li> <code>--seed &lt;int&gt;</code> Set the seed for the random number
generator. By default, the seed is set from the clock.</li>

<li> <code>--verbosity 0|1|2|3|4</code> Set the verbosity of 
output to stderr.  Default=2.</li>

</ul>

<p>
<b>Warning messages:</b> None
</p>

<p><b>Bugs:</b></p>

<p>
None known.
</p>

    </div>
  </body>
</html>
